# CPU-only torch wheel (no CUDA) — keep the image small and Railway-friendly.
# The extra index is the official PyTorch CPU wheel host.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.0

transformers==4.57.1
pillow==12.0.0
fastapi==0.115.6
uvicorn[standard]==0.34.0
numpy>=1.26,<3
# transformers needs one of safetensors/accelerate for from_pretrained; safetensors is enough.
safetensors>=0.4
